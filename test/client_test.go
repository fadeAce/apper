package test

import (
	"testing"
)

// this test is for server daemon started
// test it's functional features especially begin at Listen()
// test dispatcher of pipes and register of task
// test cushion works
// test caching layer
// test pipes and handlers
// test execution of persist
// test notifier of the workload

// mock a client to go with daemon in using apper-go sdk
func Test_client_start(t *testing.T) {

}
